GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_client_secret
GOOGLE_REDIRECT_URI=http://localhost:5173/oauth2callback
# the folder that contains Docs to publish
DRIVE_FOLDER_ID=your_drive_folder_id
# output dir (optional; defaults to dist)
OUTPUT_DIR=dist
# site title (optional)
SITE_TITLE=My Docs Site

# Get credentials: 
#   Google Cloud Console → APIs & Services → Credentials: 
#   create an OAuth 2.0 Client ID (Desktop app is fine). 
# Add the redirect URI above. Enable Google Drive API.